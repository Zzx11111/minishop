<view>
  <block wx:for="{{cartList}}" wx:key="index">
    <cart-item goods="{{item}}" bind:onCheckClick="onCheckClick"></cart-item>
  </block>
  <cart-bottom total="{{total}}" priceTotal="{{priceTotal}}" isAllSelect="{{isAllSelect}}" bind:isAllSelect="isAllSelect"></cart-bottom>
</view>